# Nova Syndicate — Credentials MariaDB backup (Ansible managed)
# Deploye par le role database, tag db:backup
# Mode 600, owner root — ne pas modifier manuellement
[client]
user     = backup_nova
password = {{ vault_mariadb_backup_password }}
