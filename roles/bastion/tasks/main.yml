---
# Role bastion - Phase II minimale
# Le SSH hardening + fail2ban + nftables sont appliques par le role hardening
# Phase II.5 ajoutera : MFA TOTP, rsyslog vers Wazuh, ansible/terraform installes

- name: Placeholder bastion - architecture finale a implementer en Phase II.5
  ansible.builtin.debug:
    msg: |
      Bastion configuration minimale (common + hardening uniquement).
      A implementer en Phase II.5 :
      - MFA TOTP via libpam-google-authenticator
      - rsyslog forward vers Wazuh APP01:514
      - Installation Ansible + Terraform sur le bastion
      - Clone du repo Git sur le bastion
      - auditd kernel-level
  tags: [role:bastion]
